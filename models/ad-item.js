class Ad {
  constructor(id, ownerId, title, image, description, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.image = image;
    this.description = description;
    this.price = price;
  }
}

export default Ad;
